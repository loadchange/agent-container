#!/bin/bash
set -euo pipefail

launcher="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd -P)/agent-container"
if [ "${1:-}" = run ]; then
  shift
  exec "$launcher" run codex "$@"
fi
exec "$launcher" codex "$@"
